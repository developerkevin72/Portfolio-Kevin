import { ScrollRevealDirective } from './scroll-reveal';

describe('ScrollRevealDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollRevealDirective({} as never);
    expect(directive).toBeTruthy();
  });
});
